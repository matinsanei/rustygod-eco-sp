{
  "name": "@rustygod/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Generated TypeScript gRPC clients for rustygod-saleor (single source of truth: crates/proto).",
  "type": "module",
  "scripts": {
    "gen": "bash ./generate.sh",
    "build": "tsc",
    "demo": "npm run build && node dist/demo/buy-flow.js"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.13.0"
  },
  "devDependencies": {
    "grpc-tools": "^1.13.0",
    "ts-proto": "^2.6.0",
    "typescript": "^5.6.0"
  }
}
