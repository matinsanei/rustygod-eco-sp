[package]
name = "saleor-rustify-proto"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"

[dependencies]
tonic = { workspace = true }
prost = { workspace = true }

[build-dependencies]
tonic-build = "0.12"
protoc-bin-vendored = "3"
